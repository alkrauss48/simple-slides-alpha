version: "3.9"

services:
  frontend:
    # image: alkrauss48/takahashi-slides:dev-main
    # platform: linux/amd64
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
    ports:
      - "8080:80"
